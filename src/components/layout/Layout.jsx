import "./styles.css";
import { Header, Form } from "../../components";

const Layout = () => {
  return (
    <div className="layout">
      <Header />
      <Form />
    </div>
  );
};

export default Layout;
