// 초기 상태값
const initialState = {
  number: 0,
};

// reducer function(변화를 일으키는 함수)
const counter = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default counter;
